# R1 teleop runtime env (conda env `r1rt`, Python 3.10; no torch, no Isaac):
#   mamba create -y -n r1rt python=3.10
#   ~/miniforge3/envs/r1rt/bin/pip install -r scripts/r1/teleop/requirements.txt
#   ~/miniforge3/envs/r1rt/bin/pip install --no-deps -e external_dependencies/unitree_sdk2_python
# cyclonedds 0.10.2's wheel bundles the C library (unitree_sdk2py pins this version).
numpy==1.26.4
scipy==1.15.3
joblib==1.6.0
pyyaml
mujoco==3.14.0
onnxruntime-gpu[cuda,cudnn]
# cuDNN >= 9.6 fails on the TITAN V (sm_70) with CUDNN_STATUS_EXECUTION_FAILED_CUDART
nvidia-cudnn-cu12==9.5.1.17
cyclonedds==0.10.2
televuer @ git+https://github.com/unitreerobotics/televuer@main
# vuer 0.0.60 (televuer's pin) imports params_proto.Flag, removed in params-proto 3
params-proto<3
pytest
